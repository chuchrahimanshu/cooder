// Import Section
import { asyncHandler } from "../../../../../utils/asyncHandler.util.js";

// Controller Actions - End Points
export const addSnippetToFavourites = asyncHandler(
  async (req, res, next) => {}
);
